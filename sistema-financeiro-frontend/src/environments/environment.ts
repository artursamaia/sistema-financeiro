/**
 * Configurações de ambiente para desenvolvimento.
 * Em produção, este arquivo seria substituído pelo environment.prod.ts.
 */
export const environment = {
  production: false,
  /** URL base da API NestJS — sem barra no final. */
  apiUrl: 'http://localhost:3000/api',
};
